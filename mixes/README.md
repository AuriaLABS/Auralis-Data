# Mixes

Train / validation / test recipes. Each mix has a name and a version.

Until the first mix exists, this directory only holds this note. When a mix is published, pair it with an entry under `manifests/` and checksums under `checksums/`.
