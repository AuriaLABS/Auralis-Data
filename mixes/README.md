# Mixes

Train / validation / test recipes. Each mix has a name and a version.

The inventory lives in [`../manifests/genesis-mix-v0.1.0.yaml`](../manifests/genesis-mix-v0.1.0.yaml).

Genesis smoke: `samples/keep_prose.txt` + `samples/keep_pii.txt` after `clean-es-v0.3.0`.
External sources are named with weight 0 until a checksum exists.
