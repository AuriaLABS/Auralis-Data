# Licenses

Per-source license texts or excerpts. The MIT license at the repo root covers scaffolding only.

Put the source id in the filename (`common-crawl.txt`, `project-gutenberg.md`). The matching dataset card must point here.
